#include<stdio.h>

int main()
{
    printf("Hill Cipher is vulnerable to known plaintext attack.\n");
    printf("If attacker knows plaintext and ciphertext pairs,\n");
    printf("the key matrix can be calculated using linear algebra.\n");

    return 0;
}